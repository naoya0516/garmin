# 課題管理ログ

開発を進める中でエージェント間・設計書間で発生した論点（矛盾・記載漏れ・判断の割れなど）について、何が問題になったか・どう議論したか・どう解決したかを記録する。`issue-discuss` スキルを実行するたびに、以下の形式で1エントリを追記する（過去エントリは書き換えない）。

```markdown
## YYYY-MM-DD: <課題タイトル>

**発生した問題**:
...

**議論**:
<1人目のエージェントの判断>

<2人目のエージェントの応答>

（意見が割れた場合は追加ラウンドをここに記載）

**結論・対応**:
...

**関連ファイル/参照**:
...
```

---

## 2026-07-17: F-03用APIエンドポイントが05_API設計.mdに未反映

**発生した問題**:

`docs/08_詳細設計.md`（実装前の詳細設計、gitタグ`v1.1.0-design`）を作成する過程で、F-03（走行サマリー表示）を実現するためのバックエンドAPIとして`GET /api/activities/summary`という新規エンドポイントを設計した。しかし確定済みの基本設計書`docs/05_API設計.md`（gitタグ`v1.0.0-design`）のエンドポイント一覧表には`GET /api/activities`と`POST /api/sync`のみが記載されており、F-03に対応するエンドポイントの記載が無かった。担当コーディングエージェントは「01〜07は変更しない」というタスク指示に従い、08側に「暫定案・要確認」と注記するに留めていたが、ユーザーからこの経緯について「なぜ基本設計と異なる実装判断をしたか」の説明を求められたことで、05を放置してよいか（＝基本設計書の修正が必要か）という論点が浮上した。

**議論**:

コンサル（garmin-consultant）の判断:
「修正すべき。ただし軽微な追記で足りる」。`01_システム概要.md`はF-03を今回のMVP対象と明記し、`02_機能一覧.md`はF-03を承認済み機能一覧に正式掲載、`03_画面設計.md`の操作フローシーケンス図は「サマリー取得(F-03)」というAPI呼び出しの発生自体を明記している。01〜03（承認済み）はF-03のAPI呼び出しを前提にしているのに契約を定める05に該当行が無いのは内部矛盾であり、「05は技術章でコーチ・情シスのレビュー対象外」なのは内容審査プロセスの範囲の話であって01〜03との整合を免除する理由にはならない。`DESIGN_REVIEW_LOG.md`を確認すると、前任コンサルが`design-review-fix`当時に既に「APIはサマリー専用の新規エンドポイント追加を推奨」と言及していたが、当時のスコープが01〜03に限定されていたため05への反映が先送りされていた経緯がある。修正内容は08が既に提示した`GET /api/activities/summary`・`ActivitySummaryOut`を転記する程度で完結し、新規のUX判断を伴わないためコーチ・情シスの再レビューは不要。パス名の技術判断（`/api/activities/summary` vs `/api/summary`）もコンサル・コーディング間の合意で足りる。

コーディング（garmin-coding）の応答:
コンサルの4点すべてに同意。実装者の立場からも「08を書く際に実際にこのギャップに直面し、独自にエンドポイントを設計する羽目になった」という実務上の裏付けを追加。前任コンサルの推奨が先送りされていた経緯も`DESIGN_REVIEW_LOG.md`で事実確認し同意。加えて2点補足: (1) 05は`v1.0.0-design`タグで確定済みのため、タグ後に追記する場合は「いつ・なぜ追記したか」が分かる記録を残すべき、(2) 実際に修正する場合はコーディング自身が転記作業を担当する進め方を提案。結論として「今回のタイミングで05_API設計.mdを修正すべき」に同意。

**結論・対応**:

ユーザー承認のもと、`docs/05_API設計.md`に`GET /api/activities/summary`のエンドポイントを追記する（コーチ・情シスの再レビューは行わない。理由は上記議論の通り、01〜03で承認済みの仕様を技術文書に転記するのみのため）。あわせて`docs/08_詳細設計.md`の「暫定案・要確認」の注記を、05へ反映済みである旨に更新する。担当はコーディング（garmin-coding）。作業内容は別途コミットログ・本エントリで追跡可能とする。

**関連ファイル/参照**:
- `docs/01_システム概要.md` `02_機能一覧.md` `03_画面設計.md`（F-03の承認根拠）
- `docs/05_API設計.md`（修正対象）
- `docs/08_詳細設計.md`（暫定案の出典）
- `DESIGN_REVIEW_LOG.md`（前任コンサルの先送り経緯）

---

## 2026-07-17: 実装着手前の懸念点洗い出し（コンサル・コーダー・コードレビュアー3者議論）

**発生した問題**:

`backend/`・`frontend/`の実装に着手する前に、現状の設計（`docs/01〜08`、`PLAN.md`）に基づいて実装を始めて問題ないか、ユーザーの指示によりコンサル・コーダー・コードレビュアーの3者で懸念点を洗い出した。

**議論**:

コンサル（garmin-consultant）の見解:
条件付きで着手可能。ただし着手前に対応すべき点が1つある。`PLAN.md`が`docs/08_詳細設計.md`に追随しておらず、ディレクトリ構成・API一覧に`summary.py`/`SummaryPanel.tsx`/`GET /api/activities/summary`/`data/`の記載がない。`AGENTS.md`は「コーディングはPLAN.mdの規約に従って実装」と明記しているため、実装者がPLAN.mdのみを見た場合にF-03を実装から落とすリスクがある。転記作業でありコーチ・情シスの再レビューは不要。加えて実装時の申し送り事項として: (1)`garminconnect`ライブラリの実フィールド構造が未検証の推測値、(2)フォーマット関数で`null`と`0`を区別しないと0埋め表示のバグになりうる、(3)`upsert_activities`の`synced_at`更新が「推奨」止まりで必須になっていない、(4)`sync_lookback_days`が28日未満だとサマリーが未同期期間を含む暗黙の前提、(5)`scripts/init_login.py`だけ他モジュールより詳細設計が薄い、(6)`requirements.txt`のバージョン非固定。F-03の優先度表現揺れ（01と02）は情シス判断済みの軽微事項として対応不要。

コーダー（garmin-coder）の応答:
すべてに同意。PLAN.mdの整合は自分（実装担当）が実装着手の最初のステップとして行うと表明。加えて、`synced_at`の明示更新やlookback前提のコード注記は自分の裁量で実装時に含めると回答。実装順序として: ①PLAN.md更新 → ②venv構築・使い捨てスクリプトで実ログイン/MFA/生JSON構造を確認 → ③requirements.txtバージョン確定 → ④init_login.py実装 → ⑤バックエンド各モジュールを依存順に実装 → ⑥フロントエンド実装（null厳密判定含む）→ ⑦docs/08記載の検証手順を実施、を提案。

コードレビュアー（garmin-code-reviewer）の応答:
上記の議論におおむね同意した上で、実装後のレビューで重点的に見るべき点を追加指摘。(a) `synced_at`は`models.py`の`onupdate=func.now()`がORM経由のUPDATEにのみ効き、`sync.py`が使うCore APIの`insert().on_conflict_do_update()`には自動適用されないため、`update_cols`への明示追加が実質必須（コンサル指摘(3)を技術的に補強）。(b) `format.ts`の関数はActivityTable（個別値）とSummaryPanel（集計値、正当に0になりうる）の両方から共用されるため、`null`/`0`の衝突が起きやすい実装になっている。(c) 同一`activity_id`がGarminから1回の取得内で重複返却された場合、`insert().values(rows).on_conflict_do_update()`が同一トランザクション内で同じ行を2回対象にしエラーになりうるが、設計書に重複除去処理の記載がない。(d) `models.py`の`raw_json: Mapped[dict]`が他のnullableカラムと異なり`| None`を欠いている型ヒントの不整合。(e) `activity_type`の`"unknown"`フォールバックと`summary.py`の`"running"`完全一致の相互作用は意図した挙動である旨のコード注記が必要。(f) `docs/08`が実装者の裁量に委ねている分岐点（SummaryPanelのnull時表示、SyncButtonのonError、App.tsxのエラー時state扱い）は、実装後に`03_画面設計.md`の要件を満たしているか個別確認が必要。なお同エージェントが挙げた「基本設計書がgitタグ未設定・全ファイル未コミット」という指摘は誤り（`v1.0.0-design`・`v1.1.0-design`タグは実際には作成済み、コミットも完了済み）で、Bash/git権限を持たないエージェントの推測に基づく誤指摘だったため対応不要と判断した。

**結論・対応**:

実装着手可。ただし着手直後にコーダーが以下を最初のステップとして実施する:
1. `PLAN.md`を`docs/08_詳細設計.md`に整合させる（ディレクトリ構成・API一覧・依存パッケージ節の転記。あわせて`docs/02_機能一覧.md`のF-03優先度表現の揺れも解消して構わない）
2. `garminconnect`の実ログイン・MFA・生JSON構造を使い捨てスクリプトで確認してから`sync.py`本実装に入る
3. `requirements.txt`は確認できたバージョンをピン留めする

実装時に対応する事項（コーダーが実装に組み込む）:
- `sync.py`の`upsert_activities`で`update_cols`に`synced_at: func.now()`を明示的に含める（Core API経由のため`onupdate`だけでは効かない）
- `format.ts`は`null`/`0`を`=== null`で明示的に区別する
- 同一`activity_id`がバッチ内で重複した場合の重複除去処理を`upsert_activities`に追加する（設計書に明記が無かったため実装時に対応）
- `models.py`の`raw_json`の型ヒントを`Mapped[dict | None]`にする
- `sync_lookback_days<28`の暗黙の前提、`"unknown"`フォールバックと`"running"`完全一致の相互作用について、該当箇所にコード注記を残す

実装完了後のコードレビュー（garmin-code-reviewer）で重点的に確認する事項:
- `synced_at`が一括upsert経路でも実際に更新されているか
- `format.ts`の`null`/`0`表示がActivityTable・SummaryPanelの両方で要件通りか
- `docs/08`が実装者の裁量に委ねていた分岐点（SummaryPanelのnull時表示、SyncButtonのonError、App.tsxのエラー時state扱い）の実装結果が`03_画面設計.md`の要件を満たしているか

**関連ファイル/参照**:
- `PLAN.md`（更新対象）
- `docs/08_詳細設計.md`（実装の主たる拠り所）
- `docs/02_機能一覧.md`（表現揺れ）
- `docs/06_外部インターフェース設計.md`（garminconnectのリスク）

---

## 2026-07-17: 着手前対応事項の実施結果と、PLAN.mdの`garth`記述の誤り判明

**発生した問題**:

上記「実装着手前の懸念点洗い出し」で合意した3つの着手前対応事項（PLAN.md整合、garminconnect実挙動確認、requirements.txtピン留め）をコーダーが実施したところ、`PLAN.md`のMFAの扱いに関する既存記述「トークンキャッシュは`garth`が自動保存する」が事実と異なることが判明した。

**議論**:

コーダー（garmin-coder）による調査結果:
`backend/`にvenvを構築し実際に`garminconnect==0.3.6`をインストールしてソースコード・docstring・バンドルREADMEを確認した結果、`docs/08_詳細設計.md`のGarmin生レスポンス→ORMマッピング表（`activityId`/`activityType.typeKey`/`startTimeLocal`等）はライブラリの型定義と完全に一致し矛盾はなかった。一方、`PLAN.md`に既に書かれていた「`client.login(tokenstore_path)` → `garth`が自動保存」という記述は、実際にインストールされたバージョンでは事実と異なっていた。`garminconnect==0.3.6`は`garth`への依存を持たず（`pip show`のRequiresに`garth`が無い）、curl_cffiベースの独自クライアントで`~/.garminconnect/garmin_tokens.json`等にトークンを保存する方式に変わっている。あわせて、トークンキャッシュが存在しない状態で`login()`を呼んだ際、`GARMIN_EMAIL`/`PASSWORD`が設定済みだとMFA不要な場合にフォールバックで新規ログインが成立してしまう可能性があり、`docs/08`が想定する「トークンキャッシュが無い場合は例外を送出する」という前提と厳密には異なりうることも分かった（`garmin_client.py`実装時に要確認）。

**結論・対応**:

ユーザー承認のもと、`PLAN.md`の「MFAの扱い」節から`garth`の記述を削除し、実際の保存方式（`~/.garminconnect/garmin_tokens.json`等）に修正した。あわせて、今回の調査で判明した経緯である旨を本エントリへの参照として明記した。「トークンキャッシュが無い場合の挙動が想定と異なりうる」点はドキュメント修正の対象にはせず、`garmin_client.py`実装時にコーダーが確認する申し送り事項として残す。

あわせて`.gitignore`に`backend/venv/` `backend/.env` `backend/data/` `backend/.garmin_tokens/` `frontend/dist/`を追加した（`backend/`ディレクトリが今回新規作成されたため）。

**関連ファイル/参照**:
- `PLAN.md`（MFAの扱い節を修正）
- `.gitignore`（backend関連の除外ルールを追加）
- `backend/requirements.txt`（`garminconnect==0.3.6`固定）

---

## 2026-07-17: フロントエンド初期化方針をcatan-game流用から独立構成に変更

**発生した問題**:

MVP実装フェーズで、`PLAN.md`の方針（フロントエンドの`vite.config.ts`/`tsconfig*.json`/`.oxlintrc.json`/`package.json`を兄弟プロジェクト`catan-game`から流用する）に従い、コーディング担当が`catan-game`の実ファイルを読んで設定を踏襲しようとしていた。作業を見たユーザーから「Garminアプリは独立したプロジェクトなので、他プロジェクトのファイルを読まない方が良いのでは」という指摘があった。

**議論**:

ユーザーとの直接のやり取りで検討（今回はエージェント間の議論ではなくユーザー・アシスタント間の意思決定）。catan-game参照の目的は「Vite+React+TSの動く設定を一から書く手間を省く」ことのみで機能的な必然性は無い一方、以下のデメリットがあると整理した:
- 個人のホビープロジェクト間で設定を揃える機能的必然性は無い（`docs/07_非機能要件.md`の位置づけとも整合）
- catan-game固有の設定（ゲーム用path alias、oxlintルール調整等）が意図せず混入し、「独立したプロジェクト」という前提と矛盾するリスクがある
- 標準の`npm create vite@latest -- --template react-ts`は公式かつ枯れた足場であり、事故りにくい
- 無関係な他プロジェクトのソースコードを実装エージェントが読む必要が生じ、非効率

ユーザーはこの整理に同意し、方針変更を承認した。

**結論・対応**:

`PLAN.md`の該当2箇所（Context節の冒頭、フロントエンド設計節）を、catan-game流用の記述から「`npm create vite@latest frontend -- --template react-ts`の標準テンプレートを使い、本プロジェクト単体で完結させる」という記述に変更した。`.oxlintrc.json`もcatan-gameを参照せず新規に最小構成で作成する方針とした。この変更を反映した上でMVP実装を再開する。

**関連ファイル/参照**:
- `PLAN.md`（Context節・フロントエンド設計節を修正）

---

## 2026-07-22: 心拍ゾーン可視化(候補1)とACWR(候補2)の技術的実現可能性・実行順序

**発生した問題**:

`COACH_CONSULT_LOG.md`の2026-07-22エントリで提案された候補1（個人基準値登録＋心拍ゾーン分布可視化）・候補2（週間負荷・ACWR）を`feature-pipeline`で実装したいが、候補1の「ゾーン別滞在時間」に必要な心拍データ粒度、候補2の「練習負荷」計算式がいずれも未定義で、そのままでは設計に着手できない状態だった。

**議論**:

コンサル（garmin-consultant）の調査結果:
`backend/venv`にインストール済みの`garminconnect`ライブラリのソースを実際に確認した。現状の同期（`get_activities_by_date`、1回のAPI呼び出しで直近30日分を取得）は活動全体の平均心拍（`averageHR`）のみを返し、時系列・ゾーン別の心拍データは含まれない。ライブラリには`get_activity_hr_in_timezones(activity_id)`（ゾーン別滞在時間、Garmin Connect側のゾーン設定に依存）、`get_activity_splits`/`get_activity_typed_splits`（ラップ単位）、`get_activity_details`（最も細かい時系列、パースコスト高）等のメソッドが存在するが、いずれも**アクティビティ1件ごとに個別のAPI呼び出しが必要**（N+1問題）。`garminconnect`は429（レート制限）をリトライ対象外として即失敗させる実装であり、この追加通信は現行の「直近30日を1コールで取得しレート制限リスクを避ける」という設計方針（`docs/06_外部インターフェース設計.md`）と衝突する。

一方、**候補2（練習負荷・ACWR）はゾーン別データが無くても成立する**という重要な発見があった。Banisterの古典的TRIMP方式は「セッション平均心拍」×「個人の最大心拍数・安静時心拍数（ΔHR比）」のみで計算可能で、既存の`average_hr`（DB既存）＋候補1で新規登録する基準値だけで実現できる。

このためコンサルは候補1を「候補1a（個人基準値の登録フォームのみ、低リスク）」と「候補1b（ゾーン別滞在時間の可視化、追加API呼び出し必須・`docs/02_機能一覧.md`の「分析機能」「アクティビティ詳細表示」に該当し現行スコープ外）」に分割することを提案。実行順序は**候補1a→候補2（簡易TRIMP+ACWR、追加Garmin通信なし）→候補1b（正式なスコープ変更判断待ち）**を推奨した。

コーチ（garmin-coach）の応答:
全面的に同意。候補2（負荷監視）と候補1b（強度配分の中身）は本来カバーする領域が異なり、1bが無くても2の価値は損なわれない。セッション平均心拍のみのTRIMP方式は学術的に確立した手法で初版として妥当だが、(a)インターバル走など間欠的高強度運動は過小評価されやすい、(b)個人基準値（特に最大心拍数）の登録精度がTRIMP精度を直接左右する、(c)ACWR自体の傷害予測力には近年懐疑的な研究もあり「絶対的な予測」ではなく「傾向把握の目安」として位置づけるべき、という3点の注記をリリース時の見せ方に含めることを推奨。

**結論・対応**:

候補1a（個人基準値登録）→候補2（簡易TRIMP方式によるACWR表示、追加のGarmin通信なし）→候補1b（心拍ゾーン滞在時間の可視化、正式なスコープ変更が必要）の順で進めることに合意。技術調査（`garminconnect`ライブラリの実際のメソッド確認）はこの議論の中でコンサルが既に実施済み。候補1a・候補2は`feature-pipeline`でそのまま設計・実装に進められる粒度に達した。候補2の初版には、間欠運動の過小評価リスク・個人基準値の精度依存・ACWRの限界について、画面上または`docs`への注記を含めることを推奨。候補1bは`docs/02_機能一覧.md`の対象外スコープに該当するため、着手する場合は別途`design-review-fix`等の正式なレビューフローとユーザー承認が必要。

**関連ファイル/参照**:
- `COACH_CONSULT_LOG.md`（2026-07-22エントリ、候補1〜4の提案元）
- `backend/app/garmin_client.py` `sync.py` `models.py`（現行の心拍データ取得・保存範囲）
- `docs/04_DB設計.md` `docs/06_外部インターフェース設計.md` `docs/08_詳細設計.md`
- `docs/02_機能一覧.md`（候補1bが該当する「対象外機能（将来検討）」）
- `NEXT_PHASE_BACKLOG.md`（候補1・候補2のバックログ項目）

---

## 2026-07-22: 候補1b実装のためのレート制限方針修正の影響分析

**発生した問題**:

前エントリで候補1b（心拍ゾーン滞在時間の可視化）は現行のレート制限回避方針（`docs/06_外部インターフェース設計.md`）と衝突するため対象外と整理したが、ユーザーから「レート制限の設計方針を修正すれば1bは実装可能か。修正の影響を分析してほしい」との追加質問があった。

**議論**（コンサルへの分析依頼、1者のみ）:

コンサル（garmin-consultant）の分析結果:
方針修正は必要条件だが十分条件ではない。`garminconnect`ライブラリを再確認したところ、(1)ゾーン別滞在時間の一括取得エンドポイントは存在せずアクティビティ1件ごとの呼び出しが構造的に必須（N+1問題はどう方針を変えても解消しない）、(2)通常API呼び出しには429のリトライ・バックオフが実装されておらず即失敗する、という2点はライブラリ/Garmin API側の制約であり方針変更では解決しない。

選択肢を3つ比較: a)同期のたびに全件一括取得（キャッシュ無し/ありの両パターン）、b)アクティビティごとのオンデマンド取得＋DBキャッシュ、c)開発者手動の背景バッチ。実データ（1同期あたり14件）を基に試算すると、a)は同期1回あたり15コールが恒久的に発生しバースト性が高く、429発生時は`POST /api/sync`全体が失敗しうる（一覧・サマリー保存は成功しているのに同期失敗と誤認される不整合UXになる）。さらにGarmin側のCloudflare等ボット検知インフラを踏まえると、短時間の連続呼び出しはF-01同期自体を巻き添えにするリスクがある。b)は常に1件・ユーザー操作起点でバーストが原理的に発生せず、失敗の影響も該当1件に閉じ、既存の「詳細なエラーハンドリングは行わない」というMVP方針をそのまま維持できる。実装コストもb)が最も低い（既存の`sync.py`/`POST /api/sync`に手を入れず新規モジュール1つで完結）。

**結論・対応**:

コンサルはb)（オンデマンド取得＋DBキャッシュ）を推奨。ただしb)を採用する場合も、`docs/06`（オンデマンド取得・429時の扱いの追記）、`docs/02`（分析機能/対象外からF-04相当として切り出し）、`docs/04`（新規カラム/テーブル）、`docs/05`（新規エンドポイント、GET系の一部がGarmin通信を伴うようになる点）、`docs/03`（新規UI要素）の**正式な基本設計書変更が必要**であり、現時点ではまだ何も合意されていない。今回はあくまで技術的選択肢の比較分析であり、着手判断・docs更新は別途ユーザーの明示的な指示と正式なレビューフロー（`design-review-fix`等）を経てから行う。

**関連ファイル/参照**:
- `ISSUE_LOG.md`（本エントリの前エントリ、候補1a/1b/2の分割・実行順序）
- `docs/06_外部インターフェース設計.md`（レート制限方針、修正対象）
- `docs/02_機能一覧.md` `docs/04_DB設計.md` `docs/05_API設計.md` `docs/03_画面設計.md`（b)採用時に正式変更が必要な箇所）
- `backend/venv/Lib/site-packages/garminconnect/__init__.py`（`get_activity_hr_in_timezones`, 2552行目）
- `backend/venv/Lib/site-packages/garminconnect/client.py`（`_run_request`のリトライ非対応, 1293-1351行目）

---

## 2026-07-22: 候補1bが無い場合、練習評価・計画策定の目標は達成可能か

**発生した問題**:

候補1b（心拍ゾーン別滞在時間の可視化）が正式なdocs変更を要する対象外スコープと判明したことを受け、ユーザーから「ゾーン別滞在時間の可視化ができない場合、目標とする分析は不可能に見えるが、どうか」という懸念が寄せられた。

**議論**（コーチへの確認、1者のみ）:

コーチ（garmin-coach）の回答:
練習評価・計画策定というゴールを候補1b（ゾーン別滞在時間）1機能だけに依存させて考えるのは狭すぎる。評価は本来「量・負荷の管理（候補2、ゾーン別データ不要）」「回復・コンディション管理（候補3）」「長期構造の設計（候補4）」という複数軸から成り、候補1bが担うのは「強度配分の精密な検証」という一軸のみ。他の3軸は候補1bなしでも独立して十分な価値を持つ。

加えて、秒単位データが無くても「セッション単位の平均心拍＋候補1aの個人基準値」を使った**セッション単位の粗いゾーン分類**（そのセッション全体がどのゾーン相当かを大まかに判定）という代替案を提示。低強度セッション（イージーラン等、意図的に一定強度を保つ練習）では平均心拍が実質的な強度をかなり正確に反映するため、非エリートランナーに最も多い失敗パターン「イージーデーを頑張りすぎる」の検知には十分機能する。弱点はインターバル走等、1セッション内で強度が大きく変動する練習で誤分類が起きること（高強度と低強度が混ざり中強度に見えてしまう）。この弱点は「計画された練習種別（インターバル系か等）」を組み合わせることで大部分緩和できる。

**結論・対応**:

候補1bが無くても、候補1a＋候補2＋候補3＋候補4＋「セッション単位の粗いゾーン分類（練習種別との組み合わせ）」で、当初の目的（練習評価・計画策定）に科学的に意味のある形で応えられるとコーチが判断。候補1b（秒単位のゾーン滞在時間）は必須の土台ではなく将来の精緻化オプションと位置づけ、まずは候補1a・候補2・「セッション単位の粗いゾーン分類」で運用を開始することを推奨。「セッション単位の粗いゾーン分類」は新規のバックログ候補として追加する（`NEXT_PHASE_BACKLOG.md`）。

**関連ファイル/参照**:
- `ISSUE_LOG.md`（候補1a/1b/2の分割エントリ、レート制限方針修正の影響分析エントリ）
- `COACH_CONSULT_LOG.md`（2026-07-22エントリ、候補1〜4の提案元）

---

## 2026-07-22: 「練習種別タグ付けとの組み合わせ」の妥当性再検証（ユーザー指摘への訂正）

**発生した問題**:

前エントリでコーチが提示した「セッション単位の粗いゾーン分類」案について、「インターバル走等の強度変動が大きい練習は練習種別の記録と組み合わせれば緩和できる」としていた点に、ユーザーから「平均心拍のみではこの弱点は致命的では。タグ付けとの組み合わせで本当に緩和できるのか」という鋭い指摘があった。

**議論**（技術確認→コーチへの再確認）:

まずコンサル（garmin-consultant）に技術確認を依頼: 現状の同期（追加API通信不要、既存の1回のコールで取得済み）のレスポンスには`averageHR`だけでなく**`maxHR`（セッション最大心拍）も既に含まれている**ことが判明（`garminconnect/typed.py`の`Activity`型に`max_hr`フィールドが定義され、`get_activities_by_date`のレスポンスに含まれる。現状DBの`raw_json`列内には保存されているが個別カラム未抽出）。個別カラムとして切り出す追加実装コストも`average_hr`と同型のFloatカラム追加のみで低い。

この事実を踏まえコーチに再確認したところ、**前回の回答の甘さを明確に認めた**: 「練習種別のタグ付けとの組み合わせ」は、実質的に心拍データによる自動判定をユーザーの自己申告に置き換えているだけで、弱点の緩和ではなく問題の先送りだったと訂正。運用面でも、強度変動が大きく誤分類リスクが高いインターバル走・レースほど疲労で記録が雑になりやすい、ファルトレクや漸増走のように「イージー/ポイント練習」に二分できない練習も多い、という点でタグ付け依存は現実的に脆弱と認めた。

その上で、`maxHR`が追加コスト無しで使えることを踏まえた改善案を提示: 「平均と最大の乖離度（正規化: (最大−平均)÷(最大−安静時)）」が小さければ単一強度セッション、大きければ強度変動セッションとして自動検知でき、タグ付けという自己申告に頼らない。ただし平均+最大の2点データはあくまで「このセッションは単一強度ではなさそうだ、と検知するスクリーニングツール」であり、「高強度区間が合計何分あったか」を再現する「計量ツール」にはなり得ないと明確に限界も認めた（10本のインターバルと坂道での一瞬の心拍上昇は、乖離度だけでは区別できない）。

**結論・対応**:

「練習種別タグ付けとの組み合わせ」案は撤回し、代わりに「平均心拍＋最大心拍（`maxHR`、追加API通信不要）の乖離度に基づく強度変動スクリーニング」を代替案として採用する。ただしこれも「検知」止まりであり「時間配分の正確な計量」はできない、という限界を明示的にユーザーに伝えた上で運用する。`NEXT_PHASE_BACKLOG.md`の該当項目をこの内容に更新する。

**関連ファイル/参照**:
- `ISSUE_LOG.md`（候補1bが無い場合の目標達成可能性エントリ、本エントリはその訂正）
- `backend/venv/Lib/site-packages/garminconnect/typed.py`（396-423行目 `Activity`型の`max_hr`フィールド、560-574行目）
- `backend/app/sync.py`（41-71行目、`max_hr`個別カラム化は未実施）
- `NEXT_PHASE_BACKLOG.md`（セッション単位の粗いゾーン分類の項目を更新）

---

## 2026-07-22: 候補1b「正確な時間配分」実現のための技術設計と実データ検証

**発生した問題**:

ユーザーが「平均+最大の乖離度スクリーニング」では不十分と判断し、正確な時間配分（心拍ゾーン別の滞在時間）を取得する解決策をコンサル・コーダーで検討するよう指示があった。

**議論**:

コンサル（garmin-consultant）の設計:
`get_activity_hr_in_timezones`（Garmin側ゾーン設定依存）・`get_activity_splits`（ラップ単位、粒度粗い）・`get_activity_details`（秒単位の生時系列、自前でゾーン判定可能）の3方式を比較し、**`get_activity_details`を推奨**。理由は今回の要件の核心が「Garmin側の集計に頼らず本アプリの個人基準値でゾーンを判定すること」であり、`get_activity_hr_in_timezones`はゾーン境界の決定権がGarmin側にあるため趣旨と矛盾するため。オンデマンド取得＋DBキャッシュ（新規`user_profile`テーブル＝候補1a本体、新規`activity_hr_zone_cache`テーブル＝生の心拍時系列も保持し基準値変更時にGarmin再取得なしで再計算可能）、新規エンドポイント`GET /api/activities/{activity_id}/hr-zones`、422（1a未登録）/503（Garmin429）/500（その他）のエラー設計を提示。候補1a（`user_profile`）が無いと1bは成立しないため実装順序は1a→1bと明記。

コーダー（garmin-coder）による実データ検証（activity_id 23626062292、実際にGarmin Connectへ1〜2回ずつ問い合わせ、429無し、検証用スクリプトは削除済みでリポジトリはクリーン）:
- `get_activity_details(maxpoly=0)`: 心拍時系列は`activityDetailMetrics`＋`metricDescriptors`のインデックス参照方式で取得可能。**`metricsIndex`は呼び出しごとに変動する**ため、列位置のハードコードは不可、毎回動的にキーマッピングする実装が必須（設計への追加注意点として発見）。サンプリング間隔は不均一（1〜10秒、平均約5秒、51分で623サンプル）で、ゾーン滞在時間の計算は「サンプル数×固定dt」ではなく実タイムスタンプ差分を重みとした積算が必要（これも設計への追加注意点）。心拍のnullは今回0件だが他列にはnull混在を確認、null安全な実装は必要。
- `maxchart`（既定2000）は上限キャップであり固定点数ではない（今回623点）。
- ペイロード約146KB、応答時間約0.49秒。オンデマンド取得として実用上問題なし。
- `get_activity_hr_in_timezones`: 実際にGarmin側ゾーン設定（98/117/137/156/176 bpm境界）が構成されていることを確認。本アプリが将来算出する個人基準値ベースの境界とズレる可能性という設計上の懸念を実データで裏付け。
- `get_activity_splits`: ラップ単位（1km）のaverageHR/maxHRは実在するが、ラップ内のゾーン遷移は按分不可という設計上の評価を裏付け。

コーダーは設計の大枠（3層分離・オンデマンド+キャッシュ・生時系列保持）に異論なしと回答。上記の動的インデックスマッピングと可変dt積算の2点を実装時の必須注意点として設計に反映すべきと提案。

**結論・対応**:

`get_activity_details`ベースのオンデマンド取得＋DBキャッシュ方式で候補1bを実現する設計が固まり、実データ検証でも技術的に実現可能であることを確認した。実装順序は候補1a（`user_profile`テーブル・登録画面）→候補1b。着手する場合、`docs/02`（対象外→正式機能化）・`docs/03`（新規UI要素）・`docs/04`（新規テーブル2つ）・`docs/05`（新規エンドポイント）・`docs/06`（オンデマンド取得・429時の扱い）の正式な基本設計書変更が必要（現時点ではまだ何も合意されていない、`design-review-fix`等の正式フローとユーザー承認が別途必要）。

**関連ファイル/参照**:
- `ISSUE_LOG.md`（候補1a/1b/2の分割、レート制限影響分析、タグ付け案の訂正、各先行エントリ）
- `backend/venv/Lib/site-packages/garminconnect/__init__.py`（`get_activity_details`, `get_activity_hr_in_timezones`, `get_activity_splits`）
- `docs/02〜06`（1b着手時に正式変更が必要な各章）
- `NEXT_PHASE_BACKLOG.md`（候補1a/1bの項目）
